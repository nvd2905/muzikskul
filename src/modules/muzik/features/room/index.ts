/**
 * Feature: room (PLACEHOLDER — Phase 1).
 *
 * Future scope (docs/SPEC.md §7.1, §7.3, §7.10): room creation/join/leave,
 * presence & participant list, host transfer. Structure per feature:
 * components · hooks · services · store · types. No implementation yet.
 */
export {};
