/**
 * Feature: queue (PLACEHOLDER — Phase 1).
 *
 * Future scope (docs/SPEC.md §7.6, §7.7): collaborative add, host remove/reorder,
 * auto-next (auto-advance), merge-by-id hydration. No implementation yet.
 */
export {};
