/**
 * Feature: playback (PLACEHOLDER — Phase 1).
 *
 * Future scope (docs/SPEC.md §7.5, docs/PLAYBACK_ENGINE.md): host transport
 * controls, the server-authoritative sync engine, drift correction, the
 * embedded player adapter. No implementation yet.
 */
export {};
